environments {
  default {
      url.google = "https://www.google.com"
      webdriver.driver=chrome
      webdriver.chrome.driver=drivers/chromedriver
    }
    dev {
      url.google = "https://www.google.com"
      webdriver.driver=chrome
      webdriver.chrome.driver=drivers/chromedriver
    }
    uat {
      url.google = "https://www.google.com"
      webdriver.driver=chrome
      webdriver.chrome.driver=drivers/chromedriver
    }
    prod {
      url.google = "https://www.google.com"
      webdriver.driver=chrome
      webdriver.chrome.driver=drivers/chromedriver
    }
}

webdriver {
  timeout = 30000
}

logging {
  level = DEBUG
}

webdriver.driver=chrome
webdriver.chrome.driver=drivers/chromedriver